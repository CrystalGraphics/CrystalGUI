# E-A1 spike, common branch @ 1.19.4 — the version this spike exists to break against.
#
# Forge 45.4.5 is 1.19.4's newest promoted build (V2's table). No parchment pin: rather than guess a
# mappings version, this node uses official names alone, which is all the compile needs.
mc.version = 1.19.4
forge.version = 45.4.5
